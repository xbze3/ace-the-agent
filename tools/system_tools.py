# I'll put system tools here

# Create directory i workspace
# Read the contents of a directory in workspace
# Don't need update contents of directory in workspace, because that's just the same as update file in workspace
# Delete directory in workspace
